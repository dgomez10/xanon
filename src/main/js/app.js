

angular.module('xanon').constant('MONGOLAB_CONFIG', {   //need to configure the MONGOLAB_CONFIG var to meet my database
  baseUrl: '/databases/',
  dbName: 'ascrum'
});

angular.module('xanon').configure('LPCntlr'), {, }  //variables need to be configured in 